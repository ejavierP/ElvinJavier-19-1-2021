@model IEnumerable<Bertonis.BLL.ViewModels.AlbumViewModel>

@{ ViewData["Title"] = "Albums"; }

<div class="container">
    <h5>Albumes</h5>

    <div class="container">
        <div class="row">
            @foreach (var album in Model)
            {
<div class="col-sm-4 py-2">
    <div class="card h-100 border-primary">
        <div class="card-body">
            <h3 class="card-title">@Html.DisplayFor(modelItem => album.Id)</h3>
            <p class="card-text"> @Html.DisplayFor(modelItem => album.Title)</p>
            <a href="@Url.Action("Photos", "Gallery", new {  id=album.Id })" class="btn btn-outline-secondary">Ver Album</a>
        </div>
    </div>
</div>}
        </div>
    </div>
</div>